#include <iostream>

int main()
{
  using namespace std;
  cout << "Hello world!" << endl;
  return 0;
}

// std::endl creates a newline
//functions cannot be defined inside other functions in c++

//parameter vs argument?
  //parameter is a variable - value of each of the arguments is copied into the parameter
//functions - perform only one task

//function parameters and variables declared inside the function body
//have local scope

//local variables are created at the top of the function
//and destroyed at the end of the function

//forward declarations - we use a declaration statement called
//a function prototype - function prototype includes return type,
//name, parameters, and semicolon

//a forward declaration tells the compiler that an identifier
//exists before it is actually defined
